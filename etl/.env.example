# ETL Webhook Configuration
ETL_WEBHOOK_URL=http://localhost:3001/webhook/events
ETL_WEBHOOK_SECRET=your-webhook-secret-here

# Postgres Configuration (Neon)
# Option 1: Use DATABASE_URL (recommended for Render/Neon)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Option 2: Use individual connection parameters
PGHOST=your-host.neon.tech
PGPORT=5432
PGDATABASE=your-database
PGUSER=your-user
PGPASSWORD=your-password
PGSCHEMA=public
PGSSLMODE=require

# Convex Configuration (for export actions)
CONVEX_DEPLOY_KEY=your-convex-deploy-key

# dbt Configuration
DBT_PROFILES_DIR=./etl

# ============================================================================
# ML Red Team Service Configuration
# ============================================================================

# Service Configuration
ML_SERVICE_HOST=0.0.0.0
ML_SERVICE_PORT=8000
ML_SERVICE_LOG_LEVEL=INFO

# Processing Configuration
ML_POLL_INTERVAL=2.0
ML_BATCH_SIZE=10
ML_MAX_WORKERS=4

# Model Configuration
ML_MODEL_DIR=./ml-service/artifacts
ML_MODEL_VERSION=v1
ML_USE_EMBEDDINGS=false

# Model Hyperparameters
ML_N_ESTIMATORS=100
ML_MAX_DEPTH=10
ML_LEARNING_RATE=0.1

# Fallback Configuration
ML_ENABLE_FALLBACK=true
