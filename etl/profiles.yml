monitr_ai:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', './data/llm_observability.db') }}"
      threads: 4
    ci:
      type: duckdb
      path: ":memory:"
      threads: 2
